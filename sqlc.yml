version: "2"
sql:
  - engine: "postgresql"
    queries: "internal/db/queries/"
    schema: "internal/db/schema/"
    gen:
      go:
        out: "internal/db/generated/"
        package: "db"
        emit_json_tags: true
        emit_interface: true
